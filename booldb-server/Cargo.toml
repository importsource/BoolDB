[package]
name = "booldb-server"
version = "0.1.0"
edition = "2021"

[dependencies]
booldb-core = { path = "../booldb-core" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
